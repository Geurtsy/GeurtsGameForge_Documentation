# Historical Migration Note: Geurts Documentation v0.8.0

**Version:** 0.8.0
**Status:** Non-normative historical note
**Required package path:** `Migrations/v0.8.0.md`

## Historical Scope

Package v0.8.0 introduced the approved, hash-validated project-root `.gitignore` payload while retaining the then-current integration layout.

This file no longer defines an executable migration. Its former transport and product-workflow details were removed or moved for v0.9.0. Do not recreate or invoke them from an older copy of this guide.

## Preserved Historical Lessons

- A missing `.gitignore` could be created only from the validated marked payload.
- A differing project-root `.gitignore` had to remain unchanged.
- Project GDD and user-authored native entries remained outside broad documentation replacement authority.

These observations are provenance only. Current `.gitignore` authorization, payload validation, matching-file no-op, differing-file preservation, and Git-index rules are owned by `GeurtsTechniques/GeurtsGitIgnoreTechnique.md`.

## Current Migration

Use `Migrations/v0.10.0.md` as the current transition checklist. The v0.9 automatic transaction/recovery proposal is obsolete. Resolve active versions, reading order, applicability, and subject ownership through `GeurtsTechniqueManifest.md`.
